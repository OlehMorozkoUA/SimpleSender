@{
    ViewBag.Title = "Home Page";
}

<script>
    //window.onload = () => {
    //    alert("Hello");
    //}
    //$(document).ready(function () {
    //    alert("Hello");
    //   // $('#myCarousel').on('slid.bs.carousel', function () {
    //   //var src = $('.active').find('img').attr('src');
    //   //alert(src);
    //   //});
    //});
</script>

<div id="myCarousel" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#myCarousel" data-slide-to="1"></li>
        <li data-target="#myCarousel" data-slide-to="2"></li>
    </ol>

    <!-- Wrapper for slides -->
    <div class="carousel-inner" style="height: 250px;">
        <div class="item active">
            <img src="~/Content/Images/IconLogin.jpg" alt="Los Angeles" style="height: 250px; margin: 0px auto;">
        </div>

        <div class="item">
            <img src="~/Content/Images/name.jpg" alt="Chicago" style="height: 250px; margin: 0px auto;">
        </div>

        <div class="item">
            <img src="~/Content/Images/Search.png" alt="New York" style="height: 250px; margin: 0px auto;">
        </div>
    </div>

    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right"></span>
        <span class="sr-only">Next</span>
    </a>
</div>

@{
    string src = "qqqq";
}

@using (Html.BeginForm("Send", "Home", FormMethod.Post))
{
    <input id="path" name="path" type="hidden" />
    <input id="btn" class="btn btn-primary" type="submit" value="OK" />
}
<script src="~/Scripts/jquery-3.3.1.js"></script>
<script>
    $(document).ready(function () {
        var src = $('.active').find('img').attr('src');

        //$('#myCarousel').on('slide.bs.carousel', function () {

        //});

        setInterval(function () {
            src = $('.active').find('img').attr('src');
        }, 100);
        $('.carousel').carousel({
            interval: 20000
        });
        $("#btn").on("click", () => {
            $("#path").val(src.substr(1).replace(/\//g, "\\"));
            @src = src.toString();
            @{
                string mapSrc = Server.MapPath("/");
                string SourcePath = System.IO.Directory.GetCurrentDirectory();
            }
        });
    });
</script>